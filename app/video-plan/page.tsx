"use client";
import {FormEvent,useState} from "react";import {Nav,Footer} from "../page";
const durations=[["15 sec","Fast social hook","1 credit"],["30 sec","Focused story","1 credit"],["60 sec","Full walkthrough","2 credits"],["90 sec","Signature film","3 credits"]];const scenes=["Arrival","The feeling","The proof","The invitation"];
export default function VideoPlan(){const[duration,setDuration]=useState(1);const[name,setName]=useState("");const[features,setFeatures]=useState("");const[audience,setAudience]=useState("Ambitious professionals");const[tone,setTone]=useState("Cinematic & warm");const[plan,setPlan]=useState<string[]|null>(null);const[copied,setCopied]=useState(false);function generate(e:FormEvent){e.preventDefault();const project=name.trim()||"Your project";const proof=features.trim()||"its most compelling details";setPlan(["Open on an irresistible first impression of "+project+"; establish the place, pace and point of view in the first three seconds.","Build an "+tone.toLowerCase()+" lifestyle moment designed for "+audience.toLowerCase()+".","Reveal "+proof+", using confident on-screen captions and a clean visual rhythm.","Finish with a simple invitation to discover "+project+", framed as the next move—not a hard sell."]);setCopied(false)}async function copyPlan(){if(!plan)return;await navigator.clipboard.writeText(plan.map((item,i)=>(i+1)+". "+item).join("\n\n"));setCopied(true)}return <main><Nav/><section className="tool-intro shell"><p className="kicker">AI VIDEO BRIEFING STUDIO</p><h1>Give every<br/><em>frame a reason.</em></h1><p>Build a distinct property-film direction in minutes.</p></section><section className="studio shell"><header className="studio-header"><div><p className="step">STEP 01</p><h2>Set the runtime</h2></div><p>Choose the size of story you want to tell.</p></header><div className="duration-grid">{durations.map(([time,hint,cost],i)=><button type="button" className={duration===i?"duration active":"duration"} onClick={()=>setDuration(i)} key={time}><b>{time}</b><span>{hint}</span><small>{cost}</small></button>)}</div><form onSubmit={generate}><div className="rule"/><header className="studio-header"><div><p className="step">STEP 02</p><h2>Shape the brief</h2></div><p>The more specific the inputs, the more useful the plan.</p></header><div className="form-grid"><label className="wide">Project name <input value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. Bayview Residences"/></label><label>Property type <select defaultValue="Luxury condominium"><option>Luxury condominium</option><option>Premium high-rise</option><option>Serviced apartment</option><option>Landed home</option><option>Commercial space</option></select></label><label>Audience <select value={audience} onChange={e=>setAudience(e.target.value)}><option>Ambitious professionals</option><option>Young families</option><option>Investors</option><option>Retirees</option><option>International buyers</option></select></label><label className="wide">The standout details <textarea value={features} onChange={e=>setFeatures(e.target.value)} placeholder="e.g. A sea view from every home, a rooftop pool, freehold ownership and five minutes to the city centre."/></label><label>Tone <select value={tone} onChange={e=>setTone(e.target.value)}><option>Cinematic & warm</option><option>Quiet luxury</option><option>Modern & energetic</option><option>Family-first</option><option>Calm & considered</option></select></label><label>Sales energy <select defaultValue="Balanced"><option>Balanced</option><option>Soft and editorial</option><option>Direct and urgent</option></select></label></div><div className="submit-row"><div><b>Your plan includes</b><span>Strategy, four scene beats and a closing call-to-action</span></div><button className="generate" type="submit">Generate video plan <b>→</b></button></div></form>{plan&&<section className="result" aria-live="polite"><div className="result-top"><div><p className="step">YOUR VIDEO PLAN</p><h3>{name||"Your project"}, in four beats</h3></div><button type="button" onClick={copyPlan}>{copied?"Copied":"Copy plan"} <b>↗</b></button></div><div className="scene-grid">{plan.map((item,i)=><article key={item}><span>0{i+1}</span><h4>{scenes[i]}</h4><p>{item}</p></article>)}</div></section>}</section><Footer/></main>}
